/** Classifier semantics version — bump to invalidate classification caches. */
export const CLASSIFIER_VERSION = '1';

/** Text rule pack version — bump when detector rules change (v2: scoped claims, correlation dedupe, ai-unspecified). */
export const RULES_VERSION = '2';

/** App version reported in exports; keep in sync with package.json/wxt manifest. */
export const APP_VERSION = '1.0.0';
